{
  "name": "flh-er-discipline-simulator",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Decision-support tool for Vanderbilt FLH employee relations. Screens a situation for legal hard stops, classifies it, and returns state-aware next steps and routing. Decision support, not legal advice.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^5.4.11"
  }
}
